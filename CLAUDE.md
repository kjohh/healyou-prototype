# healyou-prototype — Claude 工作指引

## 原型拆解成流程圖的標準流程

當被要求將任何原型拆解成流程圖（screen flow、畫面流程圖等），**必須先完成以下兩步再開始實作**：

### 第一步：程式碼審計

閱讀原型程式碼，整理出清單後輸出給使用者確認：

1. **所有實際畫面狀態**：HTML 中真實存在的 view/state 及觸發條件
2. **殘留 CSS**：只有 CSS 定義、沒有對應 HTML 元素的 class
3. **JS 動態生成的 UI**：不讀 JS 看不出來的元素（陣列生成的色塊、動態更新的數值等）
4. **狀態轉換邏輯**：動畫 timing、transition 參數、觸發條件

等使用者確認清單無誤後，再進行第二步。

### 第二步：用瀏覽器工具截圖

使用 Chrome DevTools MCP 開啟原型，逐一觸發每個狀態並截圖，作為實作的視覺參考依據。不依賴猜測，不需使用者提供截圖。

### 第三步：實作流程圖

以審計結果和截圖為依據，確保每個畫面都忠實反映原型實際樣貌。
